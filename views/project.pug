extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9

        //- Use interpolation to dynamically add the project title.
        h1=projects[id-1].project_name

        //- Use interpolation to dynamically add the project description.
        p.lead.text-light
          | #{projects[id-1].description}

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- Use an `each` iterator to replace the li tags below and interpolation to dynamically add the technologies used in each project.
        ul
          each technology in projects[id-1].technologies
            li=technology

        //- Use template literals and interpolation to add URLs to the href below so they link to the repo and the live version of each project.
        a.button.expanded(href=`${projects[id-1].live_link}`) Live Demo
        a.button.expanded(href=`${projects[id-1].github_link}`) GitHub Repo
    
    //- Replace the URLs in the src attributes below with template literals and interpolation to dynamically add three project images.  You only have to add one image.  But adding more images to show your app in different states demonstrates a level of thoroughness and professionalism that potential employers will like to see. 
    img.image(src=`${projects[id-1].image_urls[1]}`)
    img.image(src=`${projects[id-1].image_urls[2]}`)
    img.image(src=`${projects[id-1].image_urls[3]}`)



          //-   article.grid-container.portfolio-intro.portfolio-project
          //- .grid-x
          //-   .cell.small-6.medium-9.large-9
          //-     h1=project.project_name
          //-     p.lead.text-light
          //-       | #{project.description}
          //-   .cell.portfolio-meta.small-6.medium-2.large-3
          //-     h6 Technologies
          //-     ul
          //-       each technology in project['technologies']
          //-         li=technology
          //-     a.button.expanded(href=`${project.live_link}`) Live Demo
          //-     a.button.expanded(href=`${project.github_link}`) GitHub Repo
          //- img.image(src=`/static/images/Project${project.id}-1.PNG`)
          //- img.image(src=`/static/images/Project${project.id}-2.PNG`)
          //- img.image(src=`/static/images/Project${project.id}-3.PNG`)